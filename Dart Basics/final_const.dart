void main() {
  // final : Runtime constant (assigned once, value decided at runtime)
  final username = "ahsan ali";
  print(username);

  // const : Compile time constant (value is fixed before program execution)
  const PI = 3.14;
  print(PI);
}
