void main() {
  var roll_no = 9;
  // roll_no = "nine";  // var cannot change it's type
  print(roll_no);

  dynamic section = 3;
  section = "three"; // dynamic can change it's type
  print(section);
}